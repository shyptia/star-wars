import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  navigationButtonsContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 15,
  },
  pages: {
    color: '#FFFFFF',
    fontSize: 16,
    lineHeight: 20,
    fontWeight: '700',
  },
});
